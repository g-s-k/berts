language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo

stages:
  - lint
  - build

matrix:
  include:
    - stage: lint
      rust: stable
      before_script: ./ci/prelint.sh
      script: ./ci/lint.sh
    - stage: test
      before_script: ./ci/prebuild.sh
      script: ./ci/build.sh
  allow_failures:
    - rust: nightly
  fast_finish: true
